// @saas/pdf — shared pdf-lib document generators
// Build in S4:
//   generateReportPDF(violations)   — full violation report
//   generateAssessmentPDF(scan)     — good-faith compliance assessment
//   generateLetterPDF(scan, email)  — response letter template
//
// Every generated PDF MUST include the legal disclaimer and ~57% coverage disclosure.

export {}
